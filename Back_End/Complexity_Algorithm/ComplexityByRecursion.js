var ctc_arr =[];
var count;
var keywords=["void","double","int","float","String"]
var complexityByRecursion=(lineArr)=>{
     for(let i=0;i<lineArr.length;i++){
          for(let index=0;i<5;i++){
            if(lineArr[i].includes(keywords[index]) && line.includes("()") ){
                //check if parameter is an array  
                let w=line.split(lineArr[i]);
                console.log()
            }
          }
     }
}













module.exports=complexityByRecursion;